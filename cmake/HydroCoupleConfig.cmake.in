@PACKAGE_INIT@

set(HYDROCOUPLE_FOUND 1)

set_and_check(HYDROCOUPLE_INCLUDE_DIR "@PACKAGE_INCLUDE_INSTALL_DIR@")
set_and_check(HYDROCOUPLE_INCLUDE_DIRS ${HYDROCOUPLE_INCLUDE_DIR})

set_and_check(HYDROCOUPLE_LIBRARY_DIR "@PACKAGE_LIB_INSTALL_DIR@")

set_and_check(HYDROCOUPLE_LIBRARIES "@PACKAGE_LIB_INSTALL_DIR@")
set_and_check(HYDROCOUPLE_SYSCONFIG_DIR "@PACKAGE_SYSCONFIG_INSTALL_DIR@")

check_required_components("@PROJECT_NAME@")
